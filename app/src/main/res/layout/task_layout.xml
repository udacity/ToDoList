<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    tools:context="com.example.udacity.todolist.MainActivity">
    <!-- layout height above should be wrap content, so that the items aren't SO spaced out -->


    <TextView
        android:id="@+id/taskName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:padding="15dp"
        android:textColor="#000000"
        android:textSize="18sp" />
    <!-- padding = a little bit of spacing between views -->

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- will programmatically color in adapter -->
    <TextView
        android:id="@+id/priorityMarker"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="center"
        android:textAlignment="center" />


    <!--<Button
        android:id="@+id/deleteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Done"
        android:layout_gravity="end"
        android:onClick="deleteTask"/>-->


    <!--set visible if COLUMN_PRIORITY = 1,2,3 (not 0) in adapter -->
    <!--<TextView
        android:id="@+id/androidPriorityText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:padding="5dp"
        android:textColor="#0000FF"
        android:textSize="18sp" />-->


    <!--<TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Swipe to delete"
        android:layout_gravity="end"
        android:padding="5dp"/>-->


</LinearLayout>
